#!/bin/bash
set -euo pipefail

cat > /tmp/index.html <<EOF
<html>
  <body>
    <h1>${server_text}</h1>
  </body>
</html>
EOF

cd /tmp
nohup python3 -m http.server ${server_port} >/tmp/hello-world.log 2>&1 &
